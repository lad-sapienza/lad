name: TEST

on:
  push:
    branches: [ main ]
    
  workflow_dispatch:
  

  repository_dispatch:
    types: [ publish_blog ]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: ls -a via ssh
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: ls -a
          host: digilab4.let.uniroma1.it
          username: bogdani
          privateKey: ${{ secrets.DEPLOY_KEY }}