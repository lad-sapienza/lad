---
import ArticlePage from "../../components/ArticlePage.astro"
import { getCollection } from "astro:content"

export const prerender = true

export async function getStaticPaths() {
  const articles = await getCollection("notizie")
  return articles.map(article => ({
    params: { slug: article.id.replace(".md", "") },
    props: { collection: "notizie", slug: article.id.replace(".md", "") }
  }))
}

const { collection, slug } = Astro.props;
---
<ArticlePage
  collection={collection}
  slug={slug}
  backHref="/notizie/"
  backLabel="← Torna indietro alla pagina principale delle Notizie"
/>



